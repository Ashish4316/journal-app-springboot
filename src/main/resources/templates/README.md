# Templates Directory

This directory is for server-side templates such as:
- Thymeleaf templates (.html)
- FreeMarker templates (.ftl)
- Mustache templates (.mustache)

Currently, this project uses REST API only, so this directory is empty.
